# Contributing

Create a focused branch, install `.[dev]`, and run `pytest` before opening a pull request. Public API changes should include documentation and a Spark-local integration test. Keep LightGBM calls in the execution layer and do not introduce SynapseML or copied implementation code. Contributions must be compatible with the MIT license.
